/**
 * This package contains controller classes
 * for UI elements that are defined in view package.
 * Classes in this package are supposed enable
 * interaction with UI elements and to connect
 * them with the business logic.
 */
package controller;
